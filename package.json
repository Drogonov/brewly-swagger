{
  "name": "brewly-swagger",
  "version": "1.0.0",
  "description": "An swagger repo to store mocks",
  "main": "index.js",
  "scripts": {
    "generate-mocks": "node scripts/generate-mocks.js",
    "mockoon-export": "npx mockoon-cli import --input ./swagger.json --output ./mockoon-export.json",
    "build": "npm run generate-mocks && npm run mockoon-export",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@apidevtools/json-schema-ref-parser": "^13.0.5",
    "@mockoon/cli": "^9.2.0",
    "fs-extra": "^11.3.0",
    "json-schema-faker": "^0.5.9"
  }
}