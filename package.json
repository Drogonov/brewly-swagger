{
  "name": "brewly-swagger",
  "version": "1.0.0",
  "description": "An swagger repo to store mocks",
  "main": "index.js",
  "scripts": {
    "generate-mocks": "node scripts/generate-mocks.js",
    "mockoon-export": "npx openapi-to-mockoon -i swagger.json -o mockoon-export.json --dataStrategy body-file-path --dataFolder mocks",
    "build": "npm run generate-mocks && npm run mockoon-export",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "^11.3.0",
    "json-schema-faker": "^0.5.9",
    "json-schema-ref-parser": "^9.0.9"
  }
}